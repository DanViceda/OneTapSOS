<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- Back Arrow Top Right -->
        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="32dp"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:background="?attr/selectableItemBackground"
            android:contentDescription="Back to Home"
            android:layout_marginTop="8dp"
            android:src="@drawable/ic_send" />


        <!-- Profile Info Symmetrical -->
        <ImageView
            android:id="@+id/profileCircle"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginTop="32dp"
            android:src="@drawable/profile_icon"
            android:contentDescription="Profile Icon" />

        <TextView
            android:id="@+id/fullNameText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Full Name"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/primary"
            android:layout_marginTop="12dp" />

        <TextView
            android:id="@+id/emailText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="user@example.com"
            android:textSize="16sp"
            android:textColor="@color/primary"
            android:layout_marginTop="4dp" />

        <!-- Call Logs Header -->
        <TextView
            android:id="@+id/logHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Report / Call Logs"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginTop="32dp" />

        <!-- Table Styled Black Borders -->
        <TableLayout
            android:id="@+id/profileLogTable"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:stretchColumns="*">

            <TableRow android:background="@android:color/black">
                <TextView android:text="DateTime" android:padding="8dp" android:textColor="@color/white"/>
                <TextView android:text="Location" android:padding="8dp" android:textColor="@color/white"/>
                <TextView android:text="Type" android:padding="8dp" android:textColor="@color/white"/>
            </TableRow>
            <!-- Sample row -->
            <TableRow>
                <TextView android:text="07/29 01:00" android:padding="8dp"/>
                <TextView android:text="Gate 3" android:padding="8dp"/>
                <TextView android:text="Assault" android:padding="8dp"/>
            </TableRow>
        </TableLayout>

        <!-- Logout Button - Rectangle -->
        <Button
            android:id="@+id/logoutBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Logout"
            android:layout_marginTop="230dp"
            android:background="@null"
            android:backgroundTint="@color/primary"
            android:textColor="@color/white"
            android:padding="15dp"/>
    </LinearLayout>
</ScrollView>